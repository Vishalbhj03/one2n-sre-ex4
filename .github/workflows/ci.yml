name: CI - Build Test Lint Docker

on:
  push:
    paths:
      - 'src/**'
      - 'pom.xml'
      - 'Dockerfile'
      - 'Makefile'
      - 'docker-compose.yml'
  workflow_dispatch: {}

jobs:
  ci-pipeline:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Using Java + Maven already installed on the self-hosted runner

      - name: Build API
        run: make build

      - name: Run Tests
        run: make test

      - name: Lint Code
        run: make lint

      - name: Docker Login
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: make docker-login

      - name: Build Docker Image
        run: make docker-build

      - name: Push Docker Image
        run: make docker-push



